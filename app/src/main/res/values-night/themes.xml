<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Night variant of the post-splash window theme, so the window behind Compose is dark. -->
    <style name="Theme.ChessPuzzles" parent="@android:style/Theme.Material.NoActionBar" />
</resources>
